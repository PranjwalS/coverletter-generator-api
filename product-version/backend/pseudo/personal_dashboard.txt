pseudo code config specific filtering per dashboard config, works as function for endpoints in main.py when user asks for a specific dashboard; 

    backend owns the filtering logic, validates the user's actual config from the DB server-side, and returns only what they're entitled to see. 
    you never trust the client to tell you what it's allowed to fetch. just paginate the response too so you're not sending 500 jobs at once 
    like 20-50 at a time with infinite scroll or pagination on the frontend.


    Just takes as input param what dashboard id is that user wants, FIRST; it goes through user_jobs to fetch all the jobs given user has applied to or already had scored/gened in the given config
    and SECOND. goes into dashboard_configs table, fetches the exact configs, translates to code and applies through the jobs table for any new jobs not already in user_jobs
    AND then slowly sends forward 20-50 jobs for the jobs that hit (pagination frontend)

    moreover; 
    perhaps since scoring jobs is a person by person thing, hmm idk... ig ill have two endpoints ones to fetch everything already existing in the dashboard, 
    and another which runs through jobs for not already in user jobs and adds those while scoring and whatnot to those specifically ig, and ig once a day for every user,
     ill just run a job whre it auto fetches from the non already in user jobs to get added to user_jobs basically

    so maybe; (the first two run whenever a user particularly clicks on a dashboard)
    GET /dashboard/{id}/existing   → just returns user_jobs for this config, already scored
    GET /dashboard/{id}/new        → fetches fresh jobs not in user_jobs, scores them, returns ; so the new endpoint is really just "sync latest" — runs fast if nothing new, does real work if there are new jobs to score and add
    CRON daily                     → auto-runs the new job fetch + scoring for all active users


    i believe, if a user was to edit their dashboard configs immensely, then flush all history in /existing